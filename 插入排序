插入排序的分析,实现

---------------------------------------------------------------------------
分析:

    输入:一组任意顺序的数据
    输出:一组按照指定方式排列的数据
    存储方式: 列表

过程:
	1.初始状态:列表中只有一个元素,此时列表已经排序完成.
	2.假设:现在对第k个元素进行排序,只需要对k进行排序,就可以得到正确的序列.
	3.推断:对第k+1个元素进行排序,只需要对k+1进行排序.

	证明:
	    因为前k个元素已经是正确的排序,现在只需要将第k+1个元素与前面排序好的元素进行比较,然后找到合适的位置,那么整个序列就是正确的排序.
---------------------------------------------------------------------------
实现:

	def insert_sort(N):
		for i in range(1,len(N)):
			curr = N[i]
			j = i
			while j > 0 and curr < N[j-1]:
				N[j] = N[j-1]
				j -= 1
			N[j] = curr
---------------------------------------------------------------------------
时间复杂度:
			O(n^2)





